package com.melisasan2017556451.filmuygulamasi.request;

//Service class that make HTTP request

public class Service {

/*
    private static Retrofit.Builder retrofitBuilder = new Retrofit.Builder().baseUrl(Info.BASE_URL)
            .addConverterFactory(GsonConverterFactory.create());

    //singleton pattern
    private static Retrofit retrofit = retrofitBuilder.build();

    //creating an interface
    private static MovieApi movieApi = retrofit.create(MovieApi.class);

    //constructor of MovieApi
    public static MovieApi getMovieApi(){
        return movieApi;
    }
}

 */
}
//created singleton pattern in java for Retrofit Api